# 箭箭火复活 APNs

??? quote "警告！WIP"
    此项目内容正在维护或持续更新中，含有大量笔者正在测试的技术方法或尚未整理完全的思想理论，请读者知悉。

[APNs 是啥，APNs 又咋了？](./10.箭箭火+捷徑：按应用分流.md/#fn:4)

给出一[模块](https://raw.githubusercontent.com/eepsjo/s/refs/heads/m/apns)解决 APNs 的问题。

## 测试

测试软件：太鼓。

测试环境：始终连接 + **睡眠时断开**。[兜底代理配置](https://raw.githubusercontent.com/eepsjo/s/refs/heads/c/p)和[兜底直连配置](https://raw.githubusercontent.com/eepsjo/s/refs/heads/c/d)。模块的“apnS”填写“PROXY”并使用稳定低延迟的节点。分别测试太鼓不在前台但设备未息屏、以及息屏后静置半分钟的状态。

测试结果图鉴：

- “✔”表示息屏与否都能收到消息；

- “❌”表示息屏与否都不能收到消息；

- “⚠”表示息屏时收不到但不息屏时可以。

| | “配置”兜底 | 蜂窝/Wi-Fi | 是否开启模块 | 是否开启“包含 APNs” | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| ❌ | 代理 | 蜂窝 | - | 是 | |
| ✔ | 代理 | Wi-Fi | - | - | |
| ❌ | - | 蜂窝 | - | 否 | 证明蜂窝网络下该选项是刚需 |
| ❌ | 直连 | - | 否 | - | 证明……模块的必需？ |
| ❌ | 直连 | - | 是（-/N/-） | - | 证明代理 `+.push.apple.com` 才是刚需 |
| ⚠ | 直连 | Wi-Fi | 是（N/Y/Y） | - | 模块的默认选项 |
| ⚠ | 直连 | 蜂窝 | 是（N/Y/Y） | 是 | |
| ⚠ | 直连 | Wi-Fi | 是（N/Y/N） | - | 不代理 IP 段 |
| ⚠ | 直连 | 蜂窝 | 是（N/Y/N） | 是 | |
| ⚠ | 直连 | Wi-Fi | 是（Y/Y/Y） | - | 有点延迟，容易吞消息。下同 |
| ⚠ | 直连 | 蜂窝 | 是（Y/Y/Y） | 是 | |
| ⚠ | 直连 | Wi-Fi | 是（Y/Y/N） | - | |
| ⚠ | 直连 | 蜂窝 | 是（Y/Y/N） | 是 | |

## 编辑参数

### mode & url-test & fallback

`mode` 可选值有 `url-test` 和 `fallback`，默认 `fallback`。

`url-test` 可填入一个或多个代理节点或分组的名称，每个节点或分组之间用英文逗号分隔。这些节点将进行 URL 测试，测试结果最好的节点将被用来代理 APNs。不填则使用缺省值 `PROXY`。建议填写一个或多个稳定、低延迟的节点。

`fallback` 可填入一个或多个代理节点或分组的名称，每个节点或分组之间用英文逗号分隔。这些节点将在 `url-test` 中的节点都不可用时被使用。不填则使用缺省值 `DIRECT`，也建议使用缺省值。

> 手动指定固定的节点，目的是防止切换配置时/代理组切换节点时打断 APNs 长连接导致信息接收延迟。<br>直连作为回落兜底防止节点全部超时导致 APNs 完全失效（至少直连时不需要绕过 APNs 屏蔽的应用还能收到消息）。

### interval & tolerance & timeout & url

上述模式的测试用参数，具体用法参照[箭箭火使用文档](https://lowertop.github.io/Shadowrocket/)。

### init-?01st.push.apple.com

是否代理 `DOMAIN-WILDCARD,init-?01st.push.apple.com`。默认关闭（开启表示代理，关闭以切换到直连。下同）。

在兜底代理配置的 [D 规则集](https://raw.githubusercontent.com/Loyalsoldier/clash-rules/refs/heads/release/direct.txt)中存在两条直连规则

``` yaml
payload:
  - 'init-p01st.push.apple.com'
  - 'init-s01st.push.apple.com'
```

与下一条参数冲突，故创建此选项让用户自行定夺。

### +.push.apple.com

是否代理 `DOMAIN-SUFFIX,push.apple.com`。默认开启。

### IPs

是否代理 APNs 的 IP 段。默认开启。

参考了 [Apple 的官方文档](https://support.apple.com/102266)，这里说明了 APNs 的 IP 段。

---

模块配置完成后，开启关闭飞行模式一次（这是网传的方法，目的是彻底关闭再开启一次箭箭火隧道）即可复活 APNs。

## 原理

基于笔者对 APNs 屏蔽的技术原理的*猜测*：*笔者认为*，APNs 通过检测用户的 IP，再根据应用的 ID 来定向屏蔽消息推送。

## 废稿待整理

~~模块通过代理 APNs 的域名和 IP，让 APNs 服务器认为我们在境外，即和 APNs 服务器沟通的 IP 不是东大的。~~

~~根据网络环境决定是否开启“设置 - 隧道 - 包括 APNs”，具体来说，如果是纯 Wi-Fi 环境无需开启，但使用蜂窝网络时需要。~~

~~笔者也曾考虑过使用 MITM，在 APNs 直连的前提下欺骗服务器给我们发消息，但这与域前置等仅绕过审查不代理等用法的场景不同：那些场景是目的服务商没有主动屏蔽，而这个场景是 APNs 的主动屏蔽，因此必须代理。~~

~~代理 APNs 会引发一些问题：假设 PROXY 组节点全挂了，那么 APNs 也挂了，国内 App 的消息也收不到；其次 APNs 挂代理本身就会增加延迟。还是建议关闭 iPhone 对应 App 的通知（或卸载它们。既然 Apple 会主动屏蔽这些软件的 APNs，就有理由怀疑它会和 GFW 分享哪些设备在使用这些软件），使用其他设备接收信息吧（物理分流）。硬要用的话，还是参考[上一篇文章](./10.箭箭火+捷徑：按应用分流.md)结合应用分流使用比较合适。~~